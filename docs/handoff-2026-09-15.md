# Handoff — 15 Sep 2026, 19:20 MDT

Vertex YouTube scan works on GCP credits. Do not buy $5. The next cut needs `proof/source.mp4`.

## Next action (about 10 min)

1. Put the full Sept 12 MP4 at `proof/source.mp4` (camera copy or YouTube Studio download of this upload).
2. Run `python proof/cut_vertex_reel.py`
3. Open `proof/out/vertex_reel.mp4`

Cool rule: both teams at their best. Keep kills, blocks, athletic plays. Drop funny misses.

## What is true now

- Vertex Cloud Shell + `gemini-2.5-flash` + `mime_type=video/mp4` reads `https://www.youtube.com/watch?v=sbGPehFw7n8`.
- Traffic type: ON_DEMAND. Paid by GCP credits. No $5 AI Studio prepay.
- AI Studio Developer API key still 429s at CA$0 prepay.
- Local gcloud has no ADC. Scans run in Cloud Shell.
- `proof/sample.mp4` is only 08:00–10:00. Cannot cut 21:25–29:38 from it.

## Vertex scan results

- 08:00–10:00 (`480s`–`600s`): empty list in 22.1 s. 30960 video tokens, 3000 audio. File: `proof/out/vertex_hl_8_10.json`.
- 10:00–20:00 (`600s`–`1200s`): **12 keeps**, scores 7–9, left and right. File: `proof/out/vertex_hl_10_20.json`.
- 20:00–30:00 (`1200s`–`1800s`): 7 rows, 6 keep (drop net-ace). File: `proof/out/vertex_hl_20_30.json`.
- Cut script reads all `proof/out/vertex_hl_*.json` keep rows. About 17 clips after merge, ~2 min.
- JSON from the model is a list, not `{highlights:[...]}`.
- `Part.from_uri(..., video_metadata=...)` fails on Cloud Shell. Use `types.Part(file_data=..., video_metadata=...)`.
- Location that worked: `us-central1`.

## Scripts

- `proof/vertex_youtube_scan.py` — YouTube window scan. List-or-dict JSON. mime_type set.
- `proof/cut_vertex_reel.py` — 9:16 crop + concat of keep rows. Exits 1 if `source.mp4` is missing.

## Do not

- Do not buy $5 unless Vertex starts to 429.
- Do not download the YouTube file with a third-party grabber. Use the owner file.
